import XCTest
@testable import ChallengeCreateScene

final class ChallengeCreateSceneTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ChallengeCreateScene().text, "Hello, World!")
    }
}
